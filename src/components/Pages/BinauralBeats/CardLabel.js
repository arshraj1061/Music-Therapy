import React from 'react';
import classes from './CardLabel.module.css';

function CardLabel(props) {
    return (
        <span classsName={classes.label} >
            jkb
            {/* {props.text}             */}
        </span>
    );
}

export default CardLabel;